(ns {{_expr_:expand ('%:p:h:t')}}.{{_expr_:substitute(expand('%:r'), '_', '-', '')}}
  (:require [clojure.test :as t]
            [{{_expr_:expand ('%:p:h:t')}}.{{_expr_:substitute(expand('%:r'), '_test', '', '')}} as sut]))

(t/deftest {{_cursor_}})
